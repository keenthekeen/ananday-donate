import { ReceiptShippingPipe } from './receipt-shipping.pipe';

describe('ReceiptShippingPipe', () => {
  it('create an instance', () => {
    const pipe = new ReceiptShippingPipe();
    expect(pipe).toBeTruthy();
  });
});
