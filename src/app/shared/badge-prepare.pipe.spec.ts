import { BadgePreparePipe } from './badge-prepare.pipe';

describe('BadgePreparePipe', () => {
  it('create an instance', () => {
    const pipe = new BadgePreparePipe();
    expect(pipe).toBeTruthy();
  });
});
