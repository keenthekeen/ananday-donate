import { CheckStatusTextPipe } from './check-status-text.pipe';

describe('CheckStatusTextPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckStatusTextPipe();
    expect(pipe).toBeTruthy();
  });
});
