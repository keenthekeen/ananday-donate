import { ShippingTypePipe } from './shipping-type.pipe';

describe('ShippingTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ShippingTypePipe();
    expect(pipe).toBeTruthy();
  });
});
