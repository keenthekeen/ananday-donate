import { ReceiptPreparePipe } from './receipt-prepare.pipe';

describe('ReceiptPreparePipe', () => {
  it('create an instance', () => {
    const pipe = new ReceiptPreparePipe();
    expect(pipe).toBeTruthy();
  });
});
