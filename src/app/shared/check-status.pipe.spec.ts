import { CheckStatusPipe } from './check-status.pipe';

describe('CheckStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
